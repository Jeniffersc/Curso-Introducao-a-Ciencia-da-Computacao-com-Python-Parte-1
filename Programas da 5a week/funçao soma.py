# def, esse comando def é uma palavra reservada na linguagem Python que serve para, quer dizer que estou definindo novo conceito

def soma (x, y, z): #função nomeada de soma que recebe dois valores
    return x + y + z

def multiplica (x, y, z):
    return x * y * z

def nome_do_seu_time (): #não recebe nenhum parametro 
    return "SFC"

def quieta (): #funçao que nao devolve nada
    x = 10 + 20
    print ("O valor calculado é ", x)

#def total_Caracteres (x, y, z):
    #return len(x,y,z)

#def total_Caracteres (x, y, z):
    #return sum(len(x,y,z))

#def total_Caracteres (x, y, z):
    #return sum(len(x)+len(y)+len(z))

#def total_Caracteres (x, y, z):
    #return len(x)+len(y)+len(z)

# print (soma(10, 20, 30))
# print (soma(-20, 100, 200))
    
